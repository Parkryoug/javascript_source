var name = '장나라';
        
        function process(){ // process()함수는 매개변수가 없는 함수입니다.
            var kor = 100;
            var eng = 90;
            var avg = (kor + eng) / 2;
            //return 명령어는 함수를 호출한 곳으로 결과값을 반환(return)해주는 명령어 입니다.
            return avg;
        }